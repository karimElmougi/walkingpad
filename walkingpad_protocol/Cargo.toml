[package]
name = "walkingpad_protocol"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
btleplug = "0.9"
futures = "*"
tokio = { version = "*", features = ["macros", "rt-multi-thread"]}

[dependencies]
uuid = "^0.8"
bitflags = "^1.3"
thiserror = "1"
